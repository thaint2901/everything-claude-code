---
name: configure-ecc
description: あるリポジトリが実際に必要とする ECC スキルとルールを評価し — 証拠に基づく候補リストと除外理由を提示したうえで — その候補をインストールし、プロジェクトに合わせて調整します。
---

# Configure Everything Claude Code (ECC)

**この**リポジトリが必要とする ECC スキルとルールを決め、その候補だけをインストールします。

このスキルの成果物は**評価**です。根拠のある選択と、除外したものの理由の両方を指します。必要以上のスキルを入れることは実コストであり — インストール済みスキルは毎セッションのコンテキストを消費します — 除外は選択と同じく成果物です。

## 起動タイミング

- ユーザーが "configure ecc"、"install ecc"、"setup everything claude code" などと言った場合
- ここでどの ECC スキルやルールを入れる価値があるかを判断したい場合
- リポジトリが変化したため、既存の ECC インストールを再評価したい場合
- 既存の ECC インストールを検証、修正、または調整したい場合

## このスキルが行わないこと

`/project-init` はすでにプロジェクトのスタックを検出し、マニフェストからインストール計画を解決し、dry-run を実行し、承認を経てから書き込みます。**それらを再実装しないでください。** 呼び出して、その出力を証拠として使います。

このスキルが担うのは `/project-init` が行わない 2 点です：

1. **判断** — スタックマッピングは機械的です。「このリポジトリは Python だが Web 層がないので `fastapi-patterns` はここではノイズだ」という結論は導けません。
2. **調整** — インストール済みファイルを、このプロジェクトが実際に使う内容まで削り込むこと。

## 前提条件

このスキルは起動前に Claude Code からアクセス可能である必要があります。ブートストラップには2つの方法があります：

1. **プラグイン経由**: `/plugin install ecc@ecc` — プラグインがこのスキルを自動的にロードします
2. **手動**: このスキルのみを `~/.claude/skills/configure-ecc/SKILL.md` にコピーし、"configure ecc" と言って起動します

---

## ステップ 0: ECC ソースの特定

ユーザーが既に持っているチェックアウトを優先します。リモートのクローンはフォールバックであり、既定の手段ではありません。

```bash
# 1. いま ECC リポジトリの中にいるか？
git rev-parse --show-toplevel 2>/dev/null

# 2. プラグインとして導入済みか？ その場合はプラグインルートを使う。
# 3. 上記いずれも使えず、かつユーザーに伝えたうえでのみ：
git clone https://github.com/affaan-m/everything-claude-code.git /tmp/everything-claude-code
```

見つかったソースを `ECC_ROOT` に設定します。

ユーザーが fork を使っている場合、正しいソースはローカルのチェックアウトです — 上流をクローンすると、本人が動かしていないコードを黙って入れてしまいます。何かをクローンする前に必ず確認してください。

---

## ステップ 1: マニフェストから候補集合を構築する

```bash
node "$ECC_ROOT/scripts/install-plan.js" --list-components --family skill --json
```

**このファイルにスキル一覧をハードコードしないでください。** ここに書いた一覧はスキルが 1 つ追加された時点で古くなり、しかもハードコードされた一覧は候補集合を黙って狭めます — 候補の一部しか見えない評価は、評価ではありません。

ルールも同様です。言語名をここに列挙せず、実行時に `$ECC_ROOT/rules/*/` を列挙してください。

---

## ステップ 2: 各候補に情報を付与する

マニフェストだけではスキルを判断できません。各 component が持つのは `id`、`family`、`description`、`moduleIds`、`moduleCount`、`targets` のみです。評価の前に、次の 2 つの信号を結合します：

**出所** — `$ECC_ROOT/skills/<id>/SKILL.md` の `metadata.origin` を読みます。実際に使われている値には `ECC`（ファーストパーティ）、`community`、ベンダーや個人の貢献者があり、出所を一切宣言していないスキルも相当数あります。ファーストパーティのスキルと単一ベンダーのドメインスキルを、同等の推奨として並べてはいけません。

**成熟度** — `$ECC_ROOT/manifests/install-modules.json` で当該スキルを持つモジュールの `stability` を読みます。値は `stable` と `beta` です。`beta` は推奨時に明示してください。

**レガシー — 既知の限界。** マニフェストのどこにも `deprecated` フィールドは存在せず、スキルのライフサイクル方針もありません。利用できる唯一の信号は description の自由文なので、`legacy`、`superseded`、`prefer <other-skill>` を照合し、その結果を*不完全なもの*として扱ってください。網羅的な検査であるかのように見せず、報告にその旨を書きます。現状これで捕捉できる唯一の事例は、`continuous-learning-v2` に置き換えられた `continuous-learning` です。

---

## ステップ 3: リポジトリから証拠を集める

`/project-init --dry-run` を実行し、検出されたスタックの証拠と解決された計画を保持します。

`$ECC_ROOT/config/project-stack-mappings.json` を相互参照します。これはプロジェクトの指標ファイルを ECC のスキル、ルール、フック、コマンドに対応づけたものです。

そのうえで、マッピングが表現できる範囲の先をリポジトリを読んで確かめます：

- 対応づけられたフレームワークのうち、実際に使われているものはどれで、単に推移的依存として存在するだけのものはどれか？
- そのスキルが前提とする層 — Web 層、DB 層、CI パイプライン、UI — はこのリポジトリにあるか？
- 既存の `CLAUDE.md`、`.claude/rules/`、あるいはチームの既存規約が、そのスキルの追加内容をすでにカバーしていないか？
- テストとビルドのスクリプトは、このチームの実際の進め方について何を語っているか？

**ユーザーにスタックを尋ねないでください。** リポジトリが答えを持っており、尋ねること自体が評価を省いた合図になります。

---

## ステップ 4: 予算のもとで評価する

インストール済みスキルは毎セッションのコンテキストを消費します。候補リストは「当てはまりそうなものを全部選ぶ」ではなく、予算制約のあるものとして扱ってください。

候補ごとに、見つかった証拠、推奨、理由を記録します。リポジトリ内の具体的な信号が支持する場合にのみ推奨してください。「このプロジェクトは Python である」は `python-patterns` を正当化しますが、Python エコシステムのすべてのスキルを正当化はしません。

これを既に計測しているスキルと組み合わせ、作り直さないでください：

- `context-budget` — スキル、ルール、エージェント、MCP サーバーにまたがるコンテキスト消費を監査
- `skill-stocktake` — インストール済みスキルとコマンドの品質を監査

すでに ECC がインストール済みのリポジトリなら、それとの差分を取ります。追加だけでなく、支える証拠が消えたスキルの削除も推奨してください。

---

## ステップ 5: 評価を報告する

何かをインストールする**前に**評価を提示します：

```text
## ECC 評価 — <repo>

### 検出された証拠
- <信号> -> <それが意味すること>

### 推奨 (N)
| スキル | 出所 | 成熟度 | 証拠 | 理由 |

### 意図的に除外 (M)
| スキル / グループ | 除外理由 |

### 評価できなかったもの
- <入手可能なデータでは判断できなかった項目。レガシー検出の欠落を含む>
```

除外の表は埋め草ではありません。判断が下された記録であり、次回の実行を推測のやり直しではなく差分にするものです。

そのうえで `AskUserQuestion` で候補リストを確認します。質問は候補リストについてのみ行い、候補集合の全体を列挙しないでください — 収まりませんし、提示すること自体が評価の意義を損ないます。

---

## ステップ 6: インストールレベルの選択

`AskUserQuestion` を使用します：

```text
質問: "選択したコンポーネントをどこにインストールしますか？"
選択肢:
  - "ユーザーレベル (~/.claude/)" — "すべての Claude Code プロジェクトに適用されます"
  - "プロジェクトレベル (.claude/)" — "現在のプロジェクトにのみ適用されます"
  - "両方" — "共通項目はユーザーレベル、プロジェクト固有項目はプロジェクトレベル"
```

ターゲットディレクトリを設定します：

- ユーザーレベル: `TARGET=~/.claude`
- プロジェクトレベル: `TARGET=.claude`（現在のプロジェクトルートからの相対）
- 両方: `TARGET_USER=~/.claude`, `TARGET_PROJECT=.claude`

```bash
mkdir -p $TARGET/skills $TARGET/rules
```

---

## ステップ 7: 候補をインストールする

承認された各スキルについて、正しいソースルートからスキルディレクトリ全体をコピーします：

```bash
# コアスキルは .agents/skills/ にあります
cp -R "$ECC_ROOT/.agents/skills/<skill-name>" "$TARGET/skills/"

# その他のスキルは skills/ にあります
cp -R "$ECC_ROOT/skills/<skill-name>" "$TARGET/skills/"
```

グロブで得たソースディレクトリを反復処理する際、末尾スラッシュ付きのソースを `cp` に直接渡してはいけません。ディレクトリ名を宛先名として明示してください：

```bash
cp -R "${src%/}" "$TARGET/skills/$(basename "${src%/}")"
```

`SKILL.md` だけでなくディレクトリ全体をコピーしてください — いくつかのスキルは `config.json`、フック、スクリプトを同梱しています（`continuous-learning`、`continuous-learning-v2`）。

評価で選ばれたルールディレクトリを、言語別レイアウトを保ったままインストールします：

```bash
cp -r "$ECC_ROOT/rules/common" "$TARGET/rules/common"
cp -r "$ECC_ROOT/rules/<language>" "$TARGET/rules/<language>"
```

言語ルールは共通ルールの拡張です。評価が `common` なしで言語を選んでいる場合は、その旨を伝えて追加を勧めてください。

---

## ステップ 8: インストールの検証

```bash
ls -la $TARGET/skills/ $TARGET/rules/
grep -rn "~/.claude/" $TARGET/skills/ $TARGET/rules/
grep -rn "../common/" $TARGET/rules/
```

**プロジェクトレベルのインストールでは**、`~/.claude/` パスへの参照を洗い出します：

- `~/.claude/settings.json` — 問題なし。設定は常にユーザーレベルです
- `~/.claude/skills/` または `~/.claude/rules/` — プロジェクトレベルでは壊れる可能性があります
- スキルが別のスキルを名前で参照 — 参照先もインストールされたか確認します

決めつけずに確認すべき相互参照：`*-tdd` や `*-testing` のスキルは通常、対応する `*-patterns` のスキルを期待します。`continuous-learning-v2` はユーザーレベルの `~/.claude/homunculus/` ディレクトリを期待します。言語ルールは対応する `common/` を参照します。ここに書かれた一覧ではなく、実際にインストールされたファイルに照らして検証してください。

各問題は、ファイル、行、何が問題か、推奨される修正の形で報告します。

---

## ステップ 9: インストール済みファイルの調整

評価が具体的な変更になるのがこの段階です。`AskUserQuestion` を使用します：

```text
質問: "インストール済みファイルをこのプロジェクト向けに調整しますか？"
選択肢:
  - "スキルを調整" — "該当しない節を削り、インストールレベルに合わせてパスを修正"
  - "ルールを調整" — "このリポジトリのカバレッジ目標、フォーマッタ、ワークフローに合わせる"
  - "両方を調整" — "インストールした内容すべてに対して実施"
  - "スキップ" — "そのままにする"
```

すべての編集は、ステップ 3 で集めた証拠 — プロジェクト実際のテストランナー、フォーマッタ、カバレッジ目標 — に基づいて行い、ユーザーにスタックを言い直させないでください。

**重要**: 変更してよいのは `$TARGET/` 配下のファイルのみです。`$ECC_ROOT/` のソースリポジトリは決して変更しないでください。

---

## ステップ 10: 判断を記録する

評価 — 選択したもの、除外したもの、それぞれの理由 — をプロジェクト内の永続的な場所に書き出し、次回の実行がゼロからではなく差分になるようにします。

プロジェクトが `ecc-install.json` を保持しているなら、インストール内容と整合させてください。以後 `/project-init --config ecc-install.json` で同じインストールを再現できます。

要約を出力します：インストールレベルとパス、何を入れたか、何を除外しその理由は何か、検出および修正した検証上の問題、適用した調整。

---

## トラブルシューティング

### 「スキルが Claude Code に認識されない」

- スキルディレクトリに `SKILL.md` があることを確認（散在する .md だけではない）
- ユーザーレベル: `~/.claude/skills/<skill-name>/SKILL.md` の存在を確認
- プロジェクトレベル: `.claude/skills/<skill-name>/SKILL.md` の存在を確認

### 「ルールが機能しない」

- レイアウトがインストール方法と一致しているか確認：言語別なら `$TARGET/rules/<language>/`
- ルールのインストール後に Claude Code を再起動

### 「プロジェクトレベルのインストール後にパス参照エラー」

- 一部のスキルは `~/.claude/` パスを前提とします。ステップ 8 がこれらを検出します。
- `continuous-learning-v2` の `~/.claude/homunculus/` は常にユーザーレベルです — 想定どおりであり、エラーではありません。

### 「期待していたスキルが推奨されなかった」

評価はリポジトリの証拠が支持するものだけを推奨します。明示的に要求すればインストールされますが、推奨されなかったこと自体が 1 つの所見です。
